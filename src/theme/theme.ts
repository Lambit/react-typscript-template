import { createTheme } from "@mui/material/styles";

// Custom Theme

export const theme = createTheme({

    palette: {
    primary: {
        light: "#00ff09",
        main: "#ff0000",
        dark: "#7303fc",

        },
        secondary: {
            main: "#fc4a03",
        },
    },
});